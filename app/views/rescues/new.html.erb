<br>
<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h2>Enter another event when Hodor saved the day</h2>
  <br>
  <%= simple_form_for @rescue do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.input :when %>
    <br>
    <%= f.submit 'Submit', class: 'btn btn-primary' %>
  <% end %>
</div>
